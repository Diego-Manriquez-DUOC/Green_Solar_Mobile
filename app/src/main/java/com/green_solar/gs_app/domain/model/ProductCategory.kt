package com.green_solar.gs_app.domain.model

/**
 * Represents the category of a product.
 * These are assumed categories, please adjust if they don't match your backend enum.
 */
enum class ProductCategory {
    PANEL,
    BATTERY,
    ACCESORY

}
